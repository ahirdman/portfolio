import { Column, Header, Title } from '../styled';

const Hero = () => {
  return (
    <Column>
      <Header>
        <Title>ALEXANDER HIRDMAN</Title>
      </Header>
    </Column>
  );
};

export default Hero;
